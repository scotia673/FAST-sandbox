import { __decorate } from "tslib";
import { customElement } from "@microsoft/fast-element";
import { HorizontalScroll, HorizontalScrollTemplate as template, } from "@microsoft/fast-foundation";
import { ActionsStyles, HorizontalScrollStyles as styles, } from "./horizontal-scroll.styles";
/**
 * The FAST HorizontalScroll Element. Implements {@link @microsoft/fast-foundation#HorizontalScroll},
 * {@link @microsoft/fast-foundation#HorizontalScrollTemplate}
 *
 *
 * @public
 * @remarks
 * HTML Element: \<fast-horizontal-scroll\>
 */
let FASTHorizontalScroll = class FASTHorizontalScroll extends HorizontalScroll {
    /**
     * @public
     */
    connectedCallback() {
        super.connectedCallback();
        if (this.view !== "mobile") {
            this.$fastController.addStyles(ActionsStyles);
        }
    }
};
FASTHorizontalScroll = __decorate([
    customElement({
        name: "fast-horizontal-scroll",
        template,
        styles,
    })
], FASTHorizontalScroll);
export { FASTHorizontalScroll };
